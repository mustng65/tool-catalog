{
    "env": {
        "name": "#{Release_EnvironmentName}",
        "version": "#{Build_BuildNumber}",
        "releaseDate": "#{releaseDate}"
    },
    "logging": {
        "console": "#{debugMode}"
    },
    "apiServer": {
        "proxy": "#{apiServerProxy}"
    }
}